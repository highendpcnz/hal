# This app is designed to run inside the Hermes Agent venv
# (~/hermes-agent/.venv), which already ships every dependency below.
# Install these only if you want a standalone environment instead.
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
pydantic>=2
piper-tts>=1.2.0
faster-whisper>=1.0
