# Standalone HAL application dependencies. Install into the repository .venv.
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
pydantic>=2
pyserial>=3.5
piper-tts>=1.2.0
faster-whisper>=1.0
# The reflection loop experiment (reflection/reflection_loop.py) needs:
# openai>=1.40
# The Crew Manifest (speaker_id.py voiceprints) needs — see README:
# sherpa-onnx>=1.13
